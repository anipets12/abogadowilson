// Este archivo asegura que React esté disponible globalmente para todos los componentes JSX
import * as React from 'react';

// Exportamos React para que esté disponible en todo el proyecto
window.React = React;

export default React;
